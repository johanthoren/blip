{
  "schemaVersion": 1,
  "id": "nixfred.blip",
  "name": "Blip",
  "version": "0.7.1",
  "author": "Larry & Fred",
  "license": "MIT",
  "description": "iMessage in the Omarchy bar. Reads and sends through fnix over a multiplexed SSH socket — iMessage is macOS-only, so vic is a thin client and fnix is the gateway. Bar badge counts every unread; desktop toasts fire only for handles on your allowlist, because chat.db is mostly bank alerts and 2FA codes. Left-click = panel (thread list, history, compose). Middle-click = refresh. Right-click = mark all read.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Blip",
    "description": "iMessage unread count via fnix. Dimmed = fnix unreachable and the bridge is offline. Click for threads, history, and a compose box.",
    "category": "Network",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {},
    "schema": []
  }
}
